# Ensure the latest setuptools is installed
setuptools>=65.0.0

# Upgrade pip to avoid issues with older versions
pip>=24.2

# Your other dependencies (adjust versions as per your project)
streamlit==1.23.0
tensorflow==2.13.0
torch==2.0.0
